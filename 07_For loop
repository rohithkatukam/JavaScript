// program to add first n numbres
let sum = 0
let n = prompt("Enter a number")
for(let i = 0; i < n ; i++) {
  sum += (i+1)
  console.log((i + 1), "+")
}
console.log("sum of first " + n + " natural numbers are " + sum)
